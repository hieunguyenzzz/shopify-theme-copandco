<div id="{{ section.id }}" class="mt-24 page-width">
    <div>
        <h2 class="text-4xl whitespace-pre-line text-center">{{section.settings.heading}}</h2>
    </div>
    <div class="mt-12 flex space-x-6">
        {% for i in (1..3) %}
            <div class="flex-1">
                <div class="w-full relative hover:shadow-lg">
                    <div class="pt-[100%]"></div>
                    <div class="w-full h-full flex absolute inset-0 justify-center items-center bg-gray-100">
                        {{ 'collection-3' | placeholder_svg_tag }}
                    </div>
                </div>
                <div class="mt-6 px-12 space-y-[0.5em]">
                    <h4 class="text-3xl">Gemmine jade set</h4>
                    <div>gold, jade pendant</div>
                    <div>
                        <span class="font-bold text-lg">now $230</span> <span class="ml-3 line-through">$120</span>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>
</div>

{% schema %}
{
    "name": "Featured 2",
    "tag": "section",
    "settings": [
        {
            "type": "text",
            "id": "heading",
            "label": "Heading",
            "default": "DON’T MISS OUR HOT SUMMER SALE"
        }
       
    ],
    "blocks": [
       
    ],
    "presets": [
        {
            "name": "Featured 2",
            "blocks": []
        }
    ]
}
{% endschema %}